name: Build Distro Artifacts

# 🐧 Constrói a Phantom (arm64) e publica em GitHub Release.
#
# O app baixa o artefato internamente (Toolbox → Distros → Instalar) com
# barra de progresso e validação SHA-256, via PhantomMirror.PHANTOM_URL.
#
# AUTÔNOMO (t30): o QEMU estático é COMPILADO dentro deste mesmo workflow
# (cross-compile nativo x86_64 → aarch64) e embutido no tarball — NÃO depende
# da Release `qemu-aarch64` (que pode ser apagada). Uma única release
# (`distro-phantom`) alimenta o app: rootfs + kernel + initrd + qemu.
#
# Artefato gerado:  phantom.tar.gz
#   ├── rootfs.img   (ext2 — Debian bookworm arm64 com ferramentas + sysvinit)
#   ├── kernel       (vmlinuz do linux-image-arm64)
#   ├── initrd.img   (initramfs do pacote)
#   ├── qemu-system-aarch64 (QEMU estático oficial do projeto, compilado aqui)
#   └── *.rom *.bin *.efi   (ROMs do pc-bios — ex.: efi-virtio.rom; o QEMU 9.x
#                            procura no CWD do processo, e o app roda com cwd =
#                            pasta da distro → boot não morre mais no firmware)
#
# Boot no app:  -kernel kernel -initrd initrd.img -drive rootfs.img \
#               -append "root=/dev/vda rw console=ttyAMA0 console=hvc0"
# O guest sobe com sysvinit → /etc/phantom-boot.sh monta a pasta da distro
# (9p darkcode-distro) e executa o dark-code-init.sh real do app.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  phantom:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Register binfmt (qemu arm64)
        run: |
          docker run --rm --privileged multiarch/qemu-user-static --reset -p yes

      - name: Install rootfs tools
        run: |
          sudo apt-get update
          sudo apt-get install -y debootstrap xz-utils zstd binutils gnupg ca-certificates curl

      - name: Build arm64 rootfs (debootstrap --foreign + ar+tar host-side)
        timeout-minutes: 20
        run: |
          set -e
          # ── MÉTODO B: rootfs SEM dpkg/qemu nos scripts ───────────────────
          # Os 8 builds anteriores travaram 16-35min: sob o qemu-user do runner,
          # scripts de manutenção do dpkg (preinst/postinst/trigger de
          # ca-certificates, initramfs-tools, libc-bin) PENDURAM.
          # Aqui NENHUM script arm64 roda no build:
          #   1. debootstrap --foreign baixa base + extras e EXTRAI os arquivos
          #      (stage 1 — sem dpkg --configure, sem scripts de manutenção).
          #   2. Todos os .debs ficam em /var/cache/apt/archives; cada um é
          #      extraído HOST-side com ar+tar (nativo x86_64, arch-independent).
          #   3. initrd + CA bundle: feitos manualmente nos próximos passos.
          sudo debootstrap --arch=arm64 --foreign --variant=minbase \
            --include=ca-certificates,curl,wget,git,python3,openssh-client,tzdata,nano,vim-tiny,htop,file,linux-image-arm64,initramfs-tools,e2fsprogs,sysvinit-core,sysv-rc,iproute2,isc-dhcp-client,libargon2-1,libsodium23,libedit2,libmagic1,media-types \
            bookworm /tmp/rootfs http://deb.debian.org/debian
          echo "==> debs baixados: $(ls /tmp/rootfs/var/cache/apt/archives/*.deb | wc -l)"

          # Extrai todos os .debs HOST-side (ar -> data.tar.* -> rootfs).
          mkdir -p /tmp/ex
          cd /tmp/ex
          for d in /tmp/rootfs/var/cache/apt/archives/*.deb; do
            rm -rf /tmp/ex/*
            ar x "$d"
            for t in data.tar.*; do
              case "$t" in
                *.xz)  sudo tar -xf "$t" -C /tmp/rootfs ;;
                *.zst) sudo tar --zstd -xf "$t" -C /tmp/rootfs ;;
                *.gz)  sudo tar -zxf "$t" -C /tmp/rootfs ;;
              esac
            done
          done
          echo "==> rootfs extraído (ar+tar host-side, sem scripts)"

          # CA bundle (o postinst do ca-certificates não roda no build).
          curl -fsSL https://curl.se/ca/cacert.pem | sudo tee /tmp/rootfs/etc/ssl/certs/ca-certificates.crt >/dev/null
          ls -la /tmp/rootfs/etc/ssl/certs/ca-certificates.crt

          # Limpeza (stage1 não precisa de apt no guest).
          sudo rm -rf /tmp/rootfs/var/cache/apt/archives /tmp/rootfs/var/lib/apt/lists /tmp/rootfs/debootstrap
          rm -rf /tmp/ex
          echo "=== kernel:"; ls -la /tmp/rootfs/boot/vmlinuz-*
          echo "=== módulos:"; ls /tmp/rootfs/lib/modules/

      - name: Generate initrd (update-initramfs via qemu chroot)
        timeout-minutes: 15
        run: |
          # ÚNICA execução arm64 do build. update-initramfs -k é estável sob
          # qemu (comprovado em runs anteriores, ~6.5min). O passo "Register
          # binfmt" registrou qemu-aarch64 no kernel do runner, então o chroot
          # roda os binários arm64 do rootfs via binfmt.
          K=$(ls /tmp/rootfs/lib/modules | head -1)
          echo "==> initrd para kernel $K"
          sudo mount -t proc proc /tmp/rootfs/proc 2>/dev/null || true
          sudo mount -t sysfs sysfs /tmp/rootfs/sys 2>/dev/null || true
          sudo mount -t devtmpfs devtmpfs /tmp/rootfs/dev 2>/dev/null || true
          timeout --kill-after=60 600 sudo chroot /tmp/rootfs /usr/sbin/update-initramfs -c -k "$K" || \
            echo "⚠️ update-initramfs retornou erro (initrd pode não existir)"
          sudo umount /tmp/rootfs/dev 2>/dev/null || true
          sudo umount /tmp/rootfs/proc 2>/dev/null || true
          sudo umount /tmp/rootfs/sys 2>/dev/null || true
          ls -la /tmp/rootfs/boot/initrd.img-* || echo "❌ sem initrd.img"

      # ─────────────────────────────────────────────────────────────────────
      # PHP 8.2 no rootfs — HOST-side (nativo, SEM qemu-user)
      # ─────────────────────────────────────────────────────────────────────
      # No container, dpkg --unpack dos debs do php PENDURA sob qemu-user
      # (~150s por deb — 7 debs ≈ 17min de hang). Aqui baixamos os .debs do
      # php8.2 do bookworm arm64 com curl (host x86), extraímos com `ar`
      # (binutils) e despejamos o data.tar direto no rootfs. conf.d/php.ini
      # são montados manualmente (é o que o trigger do php8.2-cli fazia).
      - name: Install PHP 8.2 (host-side, sem qemu-user)
        run: |
          set -e
          cd /tmp
          rm -rf php && mkdir -p php && cd php
          curl -sL https://deb.debian.org/debian/dists/bookworm/main/binary-arm64/Packages.gz -o Packages.gz
          declare -A FN
          key=""
          while read -r line; do
            if [[ $line =~ ^Package:\ (php8.2-cli|php8.2-common|php8.2-opcache|php8.2-readline|php8.2-curl|php-cli|php-curl)$ ]]; then
              key=${BASH_REMATCH[1]}
            elif [[ $line =~ ^Filename:\ (.*)$ ]] && [[ -n "$key" ]]; then
              FN[$key]=${BASH_REMATCH[1]}
              key=""
            fi
          done < <(zcat Packages.gz)
          for p in "${!FN[@]}"; do
            echo "baixando $p -> ${FN[$p]}"
            curl -sL "https://deb.debian.org/debian/${FN[$p]}" -o "$p.deb"
          done
          ls -la *.deb
          # extrai cada .deb (ar -> data.tar.*) direto no rootfs — sem dpkg
          mkdir -p ex
          for d in *.deb; do
            rm -rf ex/* && cd ex
            ar x ../"$d"
            for t in data.tar.*; do
              case "$t" in
                *.xz)  sudo tar -xf "$t" -C /tmp/rootfs ;;
                *.zst) sudo tar --zstd -xf "$t" -C /tmp/rootfs ;;
              esac
            done
            cd /tmp/php
          done
          # conf.d (o trigger do php8.2-cli cria isso — recriado manualmente)
          sudo mkdir -p /tmp/rootfs/etc/php/8.2/cli/conf.d
          for ini in /tmp/rootfs/etc/php/8.2/mods-available/*.ini; do
            [ -e "$ini" ] && sudo ln -sf "/etc/php/8.2/mods-available/$(basename "$ini")" \
              "/tmp/rootfs/etc/php/8.2/cli/conf.d/$(basename "$ini")"
          done
          sudo cp /tmp/rootfs/usr/lib/php/8.2/php.ini-production.cli /tmp/rootfs/etc/php/8.2/cli/php.ini
          sudo ln -sf /usr/bin/php8.2 /tmp/rootfs/usr/bin/php
          echo "=== php no rootfs ==="
          ls -la /tmp/rootfs/usr/bin/php*
          ls /tmp/rootfs/usr/lib/php/20220829/*.so
          ls /tmp/rootfs/etc/php/8.2/cli/conf.d/

      - name: Grab kernel + initrd
        run: |
          cp -L /tmp/rootfs/boot/vmlinuz-* /tmp/kernel
          if ! ls /tmp/rootfs/boot/initrd.img-* >/dev/null 2>&1; then
            echo "❌ initrd.img não veio no pacote linux-image — gere com update-initramfs dentro do container"
            exit 1
          fi
          cp -L /tmp/rootfs/boot/initrd.img-* /tmp/initrd.img
          ls -la /tmp/kernel /tmp/initrd.img

      # ─────────────────────────────────────────────────────────────────────
      # T-D2/T-D4 — BOOT REAL do guest (sysvinit + bootstrap Phantom)
      # ─────────────────────────────────────────────────────────────────────
      # O kernel agora acha /sbin/init (sysvinit-core) em vez de cair no
      # /bin/sh cru. O init roda /etc/phantom-boot.sh que:
      #   1. monta a pasta da distro do app via virtio-9p (tag darkcode-distro)
      #   2. executa o dark-code-init.sh REAL vindo do app (rede, usuário,
      #      workspace 9p, phantom-agent.sh e shell de login)
      # Fallback: uma cópia do init embutida no rootfs (/usr/local/lib/phantom)
      # — usada quando o QEMU roda sem a pasta 9p do app (teste manual).
      - name: Configure guest boot (sysvinit + phantom bootstrap)
        run: |
          set -e
          sudo mkdir -p /tmp/rootfs/usr/local/lib/phantom
          # Cópia de segurança do init do app dentro do próprio rootfs
          sudo cp android/app/src/main/assets/linux/dark-code-init.sh /tmp/rootfs/usr/local/lib/phantom/dark-code-init.sh
          sudo cp android/app/src/main/assets/linux/phantom-agent.sh /tmp/rootfs/usr/local/lib/phantom/phantom-agent.sh
          sudo chmod +x /tmp/rootfs/usr/local/lib/phantom/dark-code-init.sh /tmp/rootfs/usr/local/lib/phantom/phantom-agent.sh

          # Bootstrap: monta o 9p da distro (onde o app deixa o init + config)
          cat > /tmp/phantom-boot.sh <<'EOF'
          #!/bin/sh
          # T-D4: bootstrap do guest. Monta a pasta da distro do app (9p) e
          # executa o dark-code-init.sh real; cai no fallback embutido sem 9p.
          mkdir -p /mnt/phantom
          if mount -t 9p -o trans=virtio,version=9p2000.L,msize=1048576 darkcode-distro /mnt/phantom 2>/dev/null; then
            if [ -x /mnt/phantom/dark-code-init.sh ]; then
              exec /mnt/phantom/dark-code-init.sh
            fi
          fi
          if [ -x /usr/local/lib/phantom/dark-code-init.sh ]; then
            exec /usr/local/lib/phantom/dark-code-init.sh
          fi
          echo "phantom-boot: nenhum init disponível" >&2
          exec /bin/sh
          EOF
          sudo cp /tmp/phantom-boot.sh /tmp/rootfs/etc/phantom-boot.sh
          rm -f /tmp/phantom-boot.sh
          sudo chmod +x /tmp/rootfs/etc/phantom-boot.sh

          # inittab do sysvinit: sysinit roda o bootstrap; getty em hvc0 com
          # auto-login (user) como rede de segurança depois que o shell sair.
          cat > /tmp/inittab <<'EOF'
          id:2:initdefault:
          ::sysinit:/etc/phantom-boot.sh
          ::respawn:/sbin/getty -a user -L hvc0 0 vt100
          EOF
          sudo cp /tmp/inittab /tmp/rootfs/etc/inittab
          rm -f /tmp/inittab
          echo "inittab + phantom-boot.sh configurados"

      # ─────────────────────────────────────────────────────────────────────
      # QEMU ESTÁTICO (cross-compile nativo) — embutido no tarball da distro
      # ─────────────────────────────────────────────────────────────────────
      - name: Install cross toolchain + build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-aarch64-linux-gnu libc6-dev-arm64-cross \
            ninja-build pkg-config python3 python3-pip \
            autoconf automake libtool make wget ca-certificates \
            bison flex gettext
          # glib 2.80 exige meson >= 1.1; o do Ubuntu 22.04 é 0.61.
          sudo pip3 install --break-system-packages meson

      - name: Build static deps for aarch64 (zlib, libffi, pcre2, pixman)
        run: |
          set -e
          export PREFIX=/opt/arm
          export CROSS=aarch64-linux-gnu
          sudo mkdir -p $PREFIX
          cd /tmp
          # fetch <dest> <url1> <url2…>: tenta fontes em ordem (GitHub primeiro — mais estável)
          fetch() { local d="$1"; shift; for u in "$@"; do
            if wget -q -O "$d" "$u" 2>/dev/null && [ -s "$d" ]; then echo "✓ $u"; return 0; fi
            rm -f "$d"; done; echo "❌ falhou download"; exit 1; }
          fetch zlib.tar.gz \
            "https://github.com/madler/zlib/releases/download/v1.3.1/zlib-1.3.1.tar.gz" \
            "https://zlib.net/fossils/zlib-1.3.1.tar.gz"
          tar xzf zlib.tar.gz && cd zlib-1.3.1
          CC=${CROSS}-gcc AR=${CROSS}-ar RANLIB=${CROSS}-ranlib \
            ./configure --static --prefix=$PREFIX
          make -j2 && sudo make install
          cd /tmp
          fetch libffi.tar.gz \
            "https://github.com/libffi/libffi/releases/download/v3.4.6/libffi-3.4.6.tar.gz"
          tar xzf libffi.tar.gz && cd libffi-3.4.6
          ./configure --host=${CROSS} --prefix=$PREFIX --disable-shared --enable-static
          make -j2 && sudo make install
          cd /tmp
          fetch pcre2.tar.gz \
            "https://github.com/PCRE2Project/pcre2/releases/download/pcre2-10.44/pcre2-10.44.tar.gz"
          tar xzf pcre2.tar.gz && cd pcre2-10.44
          ./configure --host=${CROSS} --prefix=$PREFIX --disable-shared --enable-static
          make -j2 && sudo make install
          cd /tmp
          fetch pixman.tar.gz \
            "https://www.cairographics.org/releases/pixman-0.42.2.tar.gz" \
            "https://gitlab.freedesktop.org/pixman/pixman/-/archive/pixman-0.42.2/pixman-0.42.2.tar.gz"
          tar xzf pixman.tar.gz && cd pixman-0.42.2
          ./configure --host=${CROSS} --prefix=$PREFIX --disable-shared --enable-static \
            --disable-arm-simd --disable-arm-neon --disable-arm-iwmmxt --disable-arm-iwmmxt2
          make -j2 && sudo make install
          cd /tmp

      - name: Build static glib for aarch64 (meson cross)
        run: |
          set -e
          export PREFIX=/opt/arm
          export PKG_CONFIG_LIBDIR=$PREFIX/lib/pkgconfig
          cat > /tmp/arm-cross.txt <<'EOF'
          [binaries]
          c = 'aarch64-linux-gnu-gcc'
          cpp = 'aarch64-linux-gnu-g++'
          ar = 'aarch64-linux-gnu-ar'
          strip = 'aarch64-linux-gnu-strip'
          pkg-config = 'pkg-config'

          [host_machine]
          system = 'linux'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'
          EOF
          cd /tmp
          fetch() { local d="$1"; shift; for u in "$@"; do
            if wget -q -O "$d" "$u" 2>/dev/null && [ -s "$d" ]; then echo "✓ $u"; return 0; fi
            rm -f "$d"; done; echo "❌ falhou download"; exit 1; }
          fetch glib.tar.xz \
            "https://download.gnome.org/sources/glib/2.80/glib-2.80.4.tar.xz" \
            "https://github.com/GNOME/glib/archive/refs/tags/2.80.4.tar.gz"
          tar xf glib.tar.xz && cd glib-2.80.4
          meson setup build --cross-file /tmp/arm-cross.txt --prefix=$PREFIX \
            --default-library=static \
            -Dnls=disabled -Dtests=false -Dinstalled_tests=false \
            -Dgtk_doc=false -Dman=false -Dlibmount=disabled -Dlibelf=disabled
          ninja -C build && sudo ninja -C build install
          cd /tmp

      # libslirp ESTÁTICO (aarch64): o `-netdev user` do app (rede SLIRP/NAT do
      # guest) exige libslirp. O QEMU 9.x não traz o slirp como submodulo — a
      # dependência é resolvida por pkg-config, e sem libslirp o build sai SEM
      # rede (QEMU recusa `-netdev user` no boot — a VM nunca sobe).
      - name: Build static libslirp for aarch64 (meson cross)
        run: |
          set -e
          export PREFIX=/opt/arm
          export PKG_CONFIG_LIBDIR=$PREFIX/lib/pkgconfig
          cd /tmp
          fetch() { local d="$1"; shift; for u in "$@"; do
            if wget -q -O "$d" "$u" 2>/dev/null && [ -s "$d" ]; then echo "✓ $u"; return 0; fi
            rm -f "$d"; done; echo "❌ falhou download"; exit 1; }
          fetch libslirp.tar.gz \
            "https://gitlab.freedesktop.org/slirp/libslirp/-/archive/v4.8.0/libslirp-v4.8.0.tar.gz" \
            "https://github.com/slirp/libslirp/archive/refs/tags/v4.8.0.tar.gz"
          tar xzf libslirp.tar.gz && cd libslirp-*
          meson setup build --cross-file /tmp/arm-cross.txt --prefix=$PREFIX \
            --default-library=static
          ninja -C build && sudo ninja -C build install
          ls -la $PREFIX/lib/libslirp.a
          cd /tmp

      - name: Build static QEMU (aarch64-softmmu, cross)
        timeout-minutes: 45
        run: |
          set -e
          export PREFIX=/opt/arm
          export PKG_CONFIG=pkg-config
          export PKG_CONFIG_LIBDIR=$PREFIX/lib/pkgconfig
          cd /tmp
          fetch() { local d="$1"; shift; for u in "$@"; do
            if wget -q -O "$d" "$u" 2>/dev/null && [ -s "$d" ]; then echo "✓ $u"; return 0; fi
            rm -f "$d"; done; echo "❌ falhou download"; exit 1; }
          fetch qemu.tar.xz \
            "https://download.qemu.org/qemu-9.1.0.tar.xz" \
            "https://github.com/qemu/qemu/archive/refs/tags/v9.1.0.tar.gz"
          tar xf qemu.tar.xz && cd qemu-9.1.0
          ./configure --cross-prefix=aarch64-linux-gnu- \
            --target-list=aarch64-softmmu --static --disable-werror --prefix=/usr \
            --enable-slirp \
            --disable-gnutls --disable-nettle --disable-gcrypt \
            --disable-gtk --disable-sdl --disable-vnc --disable-spice --disable-xen \
            --disable-libnfs --disable-smartcard --disable-usb-redir --disable-glusterfs \
            --disable-curl --disable-snappy --disable-bzip2 --disable-lzo --disable-seccomp
          make -j2
          cp build/qemu-system-aarch64 /tmp/qemu-system-aarch64
          ls -la /tmp/qemu-system-aarch64
          sha256sum /tmp/qemu-system-aarch64

      # ─────────────────────────────────────────────────────────────────────
      - name: Create rootfs.img (mkfs.ext2 -d — imagem ext2 populada direto do dir)
        run: |
          # `du -s` subconta hardlinks — usamos -l e ainda sobramos
          # 140% + 200 MB. ext2 de verdade via e2fsprogs (muito mais robusto que
          # genext2fs, que estourava "couldn't allocate a block").
          SIZE_MB=$(( $(du -sk -l /tmp/rootfs | awk '{print $1}') * 140 / 100 / 1024 + 200 ))
          echo "rootfs real: $(du -sk /tmp/rootfs | awk '{print $1}') KiB → ext2: ${SIZE_MB} MiB"
          truncate -s "${SIZE_MB}M" /tmp/rootfs.img
          sudo mkfs.ext2 -q -F /tmp/rootfs.img -d /tmp/rootfs
          ls -la /tmp/rootfs.img

      - name: Package artifact
        run: |
          cd /tmp
          # T-D1: o QEMU 9.x procura ROMs de firmware no CWD do processo — as
          # ROMs do pc-bios (efi-virtio.rom etc.) vão FLAT no tarball, junto do
          # rootfs/kernel. O app roda o QEMU com cwd = pasta da distro, então o
          # boot não morre mais com "Could not open 'efi-virtio.rom'".
          mkdir -p /tmp/roms
          find /tmp/qemu-9.1.0/pc-bios -maxdepth 1 -type f -exec cp {} /tmp/roms/ \;
          cp /tmp/roms/* ./
          tar czf phantom.tar.gz rootfs.img kernel initrd.img qemu-system-aarch64 *.rom *.bin *.efi 2>/dev/null || true
          # Garante que ao menos efi-virtio.rom entrou (a ROM exigida no boot virt)
          tar tzf phantom.tar.gz | grep -q 'efi-virtio\.rom' || { echo "❌ efi-virtio.rom ausente no tarball"; exit 1; }
          echo "=== phantom.tar.gz ==="
          ls -la phantom.tar.gz
          sha256sum phantom.tar.gz

      - name: Smoke test boot (qemu-system-aarch64 nativo no runner)
        timeout-minutes: 10
        run: |
          set -e
          # Boot REAL do guest gerado, com o qemu-system-aarch64 nativo do runner
          # (x86_64 + TCG — valida kernel + initrd + rootfs.img + init sysvinit
          # end-to-end antes de publicar). Serial em ttyAMA0 + hvc0 (getty).
          sudo apt-get install -y qemu-system-arm >/dev/null
          cd /tmp
          set +e
          timeout --kill-after=10 120 qemu-system-aarch64 \
            -M virt -cpu cortex-a72 -m 512 \
            -kernel /tmp/kernel -initrd /tmp/initrd.img \
            -drive file=/tmp/rootfs.img,format=raw,if=virtio \
            -append "root=/dev/vda rw console=ttyAMA0 console=hvc0 panic=1" \
            -nographic -no-reboot 2>&1 | tee /tmp/boot.log
          rc=${PIPESTATUS[0]}
          set -e
          echo "==> qemu rc=$rc"
          # Falha dura: kernel panic.
          if grep -q "Kernel panic" /tmp/boot.log; then
            echo "❌ KERNEL PANIC no boot:"; grep -A3 "Kernel panic" /tmp/boot.log; exit 1
          fi
          # Sucesso: kernel fez a transição para o init (prova rootfs montado +
          # initramfs ok). getty em hvc0 também é sinal de boot completo.
          if grep -q "Run /sbin/init as init process\|as init process\|Freeing unused kernel memory" /tmp/boot.log; then
            echo "✅ guest bootou: kernel → initramfs → init sysvinit"
          else
            echo "❌ boot não alcançou o init (kernel/initrd/rootfs problemáticos):"
            tail -20 /tmp/boot.log
            exit 1
          fi
          echo "--- marcadores úteis:"; grep -E "Run /sbin/init|Freeing unused|getty|login:|phantom-boot|dark-code|Panic" /tmp/boot.log | head -20

      - name: Publish as Release (tag fixa para o app)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete distro-phantom --yes 2>/dev/null || true
          gh release create distro-phantom /tmp/phantom.tar.gz \
            --title "Phantom rootfs (arm64) $(date +%Y-%m-%d)" \
            --notes "Phantom — Debian bookworm arm64 para QEMU (virt/cortex-a72). Contém rootfs.img + kernel + initrd.img + qemu-system-aarch64 (compilado no próprio build). O app baixa um pacote unificado (Toolbox → Distros)."
